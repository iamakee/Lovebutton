# Assets

Put your photographs here when you are ready. Then replace each `imageLabel` in `data/memories.ts` with an image path and render it with Next's `Image` component.

The initial design intentionally uses soft placeholders so no personal photos are hardcoded.
